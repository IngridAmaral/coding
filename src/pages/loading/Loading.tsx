import { Container, IconContainer } from "./Loading.styled";
import { Load } from "@icons";

const Loading = () => (
  <Container>
    <IconContainer>
      <Load />
    </IconContainer>
    Loading
  </Container>
);

export default Loading;
